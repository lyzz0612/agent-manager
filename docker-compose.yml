services:
  agent-manager:
    image: ${IMAGE_NAME:-ghcr.io/OWNER/agent-manager}:${IMAGE_TAG:-latest}
    ports:
      - "${PORT:-3000}:3000"
    environment:
      APP_ENV: production
      ADMIN_TOKEN: ${ADMIN_TOKEN:?ADMIN_TOKEN is required}
